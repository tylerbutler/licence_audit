package = "licence_audit"
kind = "Fixed"
body = "Embed the licence_audit build version at compile time and use it for CycloneDX `metadata.tools[].version` in `sbom` output. Previously the tool read `gleam.toml` from the current working directory, so auditing another project could leak that project's version into the tool metadata instead of licence_audit's own."
