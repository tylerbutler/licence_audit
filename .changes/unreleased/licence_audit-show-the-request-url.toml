package = "licence_audit"
kind = "Fixed"
body = "Show the request URL and available DNS, connection, TLS, and timeout details when Hex metadata requests fail. Include the package and failure reason in audit and `update` progress errors."
